\begin{worktt} Continue from here
\end{worktt}
The first of these holds because
\begin{align*}
z_1 &= 
        && \mbox{,} \\ 
    &= 
        && \mbox{,} \\ 
    &= 
        && \mbox{,} \\ 
    &= 
        && \mbox{,} \\
\end{align*}
The second because
\begin{align*}
z'_1 &= \rst{z'_1} \circ z'_1  \circ \widehat{z'_1} 
                && \mbox{trivially, by R.1 and RR.2,}\\
         &= \rst{z_2} \circ z'_1  \circ \widehat{z_1} 
         &&\mbox{because $Z$ is tight,}\\
         &\leq \rst{z_2} \circ f'_1  \circ \widehat{f_1}
         &&\mbox{because $Z \leq F$ ,}\\
         &\leq \rst{t_2} \circ f'_1  \circ \widehat{f_1}
         &&\mbox{because $z_2 \leq t_2$ is established
                               above in (\ref{RLinequalityOne}).}
\end{align*}

\begin{oldtt}
We prove this piecemeal. 
For the initial segment as far as $q_{n-1}$ 
Because $\tuple{q_1,...q_{n+m}}$ is the tightening of 
$F :: G$, it follows first that
\[
\zigzagTuple{q}{n-1} \leq F^\flat
\]
and then that
\[
\zigzagTuple{q}{n-1} \leq T(F)^\flat
\] 
and, in particular, that for $1 \leq i \leq n$, $q_i < t_i$ and $q'_i < t_i$.
A similar argument gets us
\[
\zigzagTailTuple{q}{n}{m} \leq T(G)
\]
and, in particular, that 
for $1 \leq j \leq m$, $q_{n+j} < s_j$ and 
for $0 \leq j \leq m-1$, $q'_{n+j} < s'_j$.
\end{oldtt}
\begin{oldtt}
That leaves us having to prove that $q_n \leq t_n \circ s_1$.
Now consider that $F :: \tuple{g_1}$ the zigzag that is
the initial segment of the zigzag $F :: G$
that is $\tuple{f_0,...f'_{n-1},f_n \circ g_1}$
and that $\tuple{f_n} :: G$ is the zigzag that is 
the final segment of $F :: G$
that is $\tuple{f_n \circ g_1,g'_1,...g_m}$
and consider the tightenings $T(F :: g_1)$ and $T(f_n :: G)$ of these zigzags.
Because $\tuple{q_1,...q_{n+m}}$ is the tightening of 
$F :: G$ it follows
using observation \lref{biggeristighter}  that both
\begin{equation}
\label{qTFg}
\tuple{q_1,...q'_{n-1},q_n} \leq T(F :: \tuple{g_1})
\end{equation} 
and
\begin{equation}
\label{qTfG}
\tuple{q_n, q'_n,...q_{n+m}} \leq T(\tuple{f_n} :: G)
\end{equation}
By corollary \lref{tailCorollary} 
the final element of $T(F :: \tuple{g_1})$ is $ \leq t_n \circ g_1$ .
Therefore from (\ref{qTFg}) we have in particular that
\begin{equation}
\label{qgtn}
q_n \leq t_n \circ g_1.
\end{equation}
Likewise using corollary \lref{headCorollary} 
we have that the initial element
of $T(\tuple{f_n} :: G)$ is $\leq f_n \circ s_1$ and therefore from 
(\ref{qTfG}) we have the particular case that
\begin{equation}
\label{qsfn}
q_n \leq f_n \circ s_1.
\end{equation}
Finally we use lemma \lref{pointwiseorderingsCompose} to establish  from
(\ref{qgtn}) and (\ref{qsfn}) that
$q_n \leq  t_n \circ s_1$ and so complete the proof.
\end{oldtt}