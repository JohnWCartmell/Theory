\subsection{Lemma 2C  --- Representation of Referential Inclusion Dependencies}

\begin{lemma}
\label{catincdsrepresented}
If $\catc$ is a locally finite range category and $\reqtc$ is a set of instances, if $\catc$ is 
\term{maximally constrained} to the requirement $\reqtc$ then
every referential inclusion dependency with respect to $\reqtc$ is represented in $\catc$.
\end{lemma} 
\begin{proof}
Suppose such  a range category $\catc$ and  requirement $\reqtc$ 
 and suppose there is a referential inclusion dependency
$a[f_1,...f_n] \overset{R}{\subseteq} b[q_1,..q_n]$ with respect to $\reqtc$,
where
$
\begin{array}{c p{0.25cm} c  p{0.25cm} c }
             &&   \Rnode{b1}{b_1} &&              \\[0.4cm]
\Rnode{a}{a} &&                   && \Rnode{c}{c} \\[0.4cm]
             &&   \Rnode{bn}{b_n} &&              
\end{array} 
$
\ncarr{a}{b1}
\alabel{f_1}
\ncarr{c}{b1}
\blabel{q_1} 
\ncarr{a}{bn}
\blabel{f_n}
\ncarr{c}{bn}
\alabel{q_n}
in $\catc$. 

\newcommand{\barffn}{\restrictToSource{f}{n}}

From sketch $S$ of $C$ we can construct a sketch $S'$ by formally adding a morphism $\qq{r}: a \morph c$
and path equivalences, 
for each $i$, $1 \leq i \leq n$, $\qq{r} \circ q_i = \overline{\qq{r}} \circ f_i$ and
$\overline{\qq{r}}=\barffn$.
Let $\catcp$ be the range category generated by $S'$ and
let $I: C \morph C'$ be the inclusion functor generated 
by the inclusion of $S$ in $S'$. 
$I$ is consistent with $\reqtc$ since
each instance $D \in  \reqtc$ can be uniquely extended to a functor $D': \catcp \morph \Par$ by defining $D'(\qq{r})=R_D$.
\commentary{go infinite - avoid stcikleback category having to be locally finite!!}

Since \catcw is maximally constrained and because $I$ is consistent with $\reqtc$ then the functor 
$K_a$\commentary{\highlight{ $K_a$ defined} in carry forward}  can be extended 
to a functor $K'_a: C' \morph \Fin$ so that the following diagram of functors commutes 
\[
\begin{array}{c p{0.3cm} c p{0.3cm} c}
\Rnode{l}{\catc} &&                   &&     \Rnode{r}{\Par} \\[0.7cm]
                 && \Rnode{b}{\catcp} &&
\end{array}
\begin{arrows}
\ncarr{l}{r}
\alabel{K_a}
\ncarr{l}{b}
\blabel{I}
\ncarr{b}{r}
\blabel{K'_a}
\end{arrows}
\] 

By lemma \lref{leastFactorLemma}, for each $i$ we have
\[
\qq{r} \circ \restrictToSource{q}{n} \circ q_i = \qq{r} \circ q_i = \barffn \circ f_i
\]

therefore, for each $i$, this diagram commutes in in \catcp

\[
\begin{array}{c p{1.5cm} c}
\bOb{a}  && \Ob{b}{i} \\[0.7cm]
\bOb{c}  && \Rnode{c2}{c} \\[0.3cm]
\end{array}
\begin{arrows}
\ncarr{a}{bi}\alabel{\barffn f_i}
\ncarr{a}{c}\blabel{\qq{r}}
\ncarr{c}{c2}\blabel{\restrictToSource{q}{n}}
\ncarr{c2}{bi}\blabel{q_i}
\end{arrows}
\]
applying functor $K'_a$
and using the fact that $K'$ extends $K$ 
we infer this diagram commutes in \Par
\[
\begin{array} {{c p{2cm} c}}
\nudgeup{0.5cm}\Rnode{a}{\HomStk[a][a]}  && \Rnode{b}{\HomStk[a][b_i]} \\[0.7cm]
\Rnode{c}{\HomStk[a][c]}  && \Rnode{c2}{\HomStk[a][c]} \\[0.5cm]
\end{array}
\begin{arrows}
\ncarr{a}{b}\alabel{K_a(f_i)}
\ncarr{a}{c}\blabel{K'_a\qq{r})}
\ncarr{c}{c2}\blabel{K_a(\restrictToSource{q}{n})}
\ncarr{c2}{b}\blabel{K_a(q_i)}
\end{arrows}
\]


Now since there is singleton stickleback $\tuple{\barffn}: a \morph a$ in 
  $\Stk$ the commutivity of this diagram gives
\begin{equation}
\label{refdependencyPreliminary}
K'_a(\qq{r}) (\tuple{\barffn}) \circ \tuple{\restrictToSource{q}{n}} \circ \tuple{q_i} 
= \tuple{\barffn} \circ \tuple{\barffn \circ f_i}
= \tuple{\barffn \circ f_i} 
\end{equation}
in the stickleback category $\Stk$.

Let $G$ by the stickleback $K'_a(\qq{r}) (\tuple{\barffn}) \circ \tuple{\restrictToSource{q}{n}}$ so that (\ref{refdependencyPreliminary}) can be rewritten as
\begin{equation}
\label{refdependencyRewritten}
G \circ \tuple{q_i} = \tuple{\barffn \circ f_i} 
\end{equation}
and because GIVE A REASON $G \circ \tuple{q_i} = G :: q_i$
this then 
\begin{equation}
\label{refdependencyRewritten}
\tuple{g_1,g'1,...g_{m-1},g'_{m-1,g_m \circ q_i}} = \tuple{\barffn \circ f_i} 
\end{equation}

\commentary{I am working hard \workt{not to assume} that $q_i$ is \workt{total} . This seems possible with as yet rewrite of definition of referential inclusion dependency. 31 March 2026.}

From the 
sequential shortening, lemma \lref{sequentialshorteninglemma},
 we can deduce that there are
(shortening) morphisms $\sm[-1]$ so that, \foreachj[m-2],
the domain of  $s_j$ is the codomain of $g_j$ and 
the codomain  is $b_i$ such that 
\[
g_1 \circ s_1 = f_i
\]
and \foreachj[m-1]
\[
g'_j \circ s_j = g_{j+1} \circ s_{j+1}
\]
and
\[
g'_{m-1} \circ s_{m-1} = g_m \circ q_i
\] 

In any instance $D \in \reqtc$ therefore we get
\[
D(g_1) \circ D(s_1) = D(f_i)
\]
and \foreachj[m-2]
\[
D(g'_j) \circ D(s_j) = D(g_{j+1}) \circ D(s_{j+1})
\]
and
\[
D(g'_{m-1}) \circ D(s_{m-1}) = D(g_m) \circ D(q_i)
\]

From the latter two, and by use of  lemma \lref{threesidedlemma} we get
\[
D(s_j) = D(g'_j)^{-1} \circ D(g_{j+1}) \circ D(s_{j+1})
\]
and
\[
 D(s_{m-1}) = D(g'_{m-1})^{-1} \circ D(g_m) \circ D(q_i)
\]
This enables us to iteratively remove instance of $s_j$ from the identity (first of last three)
to obtain 
\[
D(g_1) \circ D(g'_1)^{-1} \circ ... 
D(g'_{m-1})^{-1} \circ D(g_m)  \circ D(q_i)
= D(\restrictToSource{f}{n} \circ f_i)
\]

Thus $D(g_1) \circ D(g'_1)^{-1} \circ ... D(g'_{m-1})^{-1} \circ D(g_m)$
is a relation in \Rel , call it $G_D$, such that $G_D \circ D(q_i) = D(f_i)$, \foreachi. 
But there is a unique single valued relationship $R_D$
such that  $R_D \circ D(q_i) = D(f_i)$. 
Therefore by lemma \lref{atleasttwopartialfunctionslemma} we have that $G_D$ is single-valued. We have that in each $D$,
\[
D(g_1) \circ D(g'_1)^{-1} \circ ... D(g'_{m-1})^{-1} \circ D(g_m) = R_D
\]
which is to say that
\[
D^*(G)=R_D
\]
and in particular $D^*(G)$ is single valued from which it follows from corollary \lref{functionalDependencyRepresentationCorollary} 
, because $G$ is tight, that $G$ is of length $1$ and
is $\tuple{g}$ 
for some morphism $g:a \morph c$ in \catcw
for which $D(g)=R_D$. We have proved that 
for all $D \in reqt$, $R_D$ is represented in \catc. 

\end{proof}

